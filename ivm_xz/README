To build a bootable image that decompresses an executable file and executes
it, issue

  make

The resulting bootable image is in mix.b.

A different file can be embedded by setting the TARGET variable in Makefile.
